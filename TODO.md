# TODO

## Layers
- [ ] Remove double click to set active layer; instead, click on the right-hand icon
- [ ] Single click on a layer selects it (currently requires double-click)
- [ ] Default layer name should be "Floor" (Floor 1, Floor 2, etc.)
- [ ] Shown-but-transparent (overlay) layers should NOT be interactive

## Selection
- [ ] Shift-click to select multiple buildings at once
- [ ] Click and drag on empty canvas (pointer mode) draws a selection box — selects all buildings within it
- [ ] When multiple buildings are selected: moving is allowed, rotating is not
- [ ] Delete / Backspace key deletes all currently selected buildings

## Viewport
- [ ] Zoom to 25% by default
- [ ] Scrolling should re-center what was centered before (zoom to screen center, not cursor)

## Other
- [ ] Snapping of objects should only occur to other objects, NOT the grid
- [ ] Save and Load buttons in the toolbar — state should be in memory
